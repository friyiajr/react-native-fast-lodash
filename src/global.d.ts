declare module global {
  function jsiCeil(num: number, precision: number): number;
  function jsiFloor(num: number, precision: number): number;
  function jsiMax(num: number[]): number;
  function jsiMin(num: number[]): number;
}
